<h3 class="text-4xl text-center font-semibold leading-normal mb-2 text-blueGray-700 mb-2"><%= @category.description %></h3>

<%= render 'tasks/task', obj: @category.tasks %>
<%= link_to 'Delete this Category', category_path(@category), method: :delete, data: {confirm: "Delete this category?"} %>