<% @category.errors.any? %>
  <ul>
    <% @category.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>




<div class="border-2 border-red-500 bg-white rounded shadow p-6 m-4 w-full lg:w-3/4 lg:max-w-lg md:max-w-2xl">
    <%@category.errors.any? %>
  <ul>
    <% @category.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
      <div class="mb-4">
        <%= form_with model: @category do |form| %>
        <div class="flex-col mt-4">
        <%= form.label :description %>
        <%= form.text_field :description, class:"border-2 border-red-500 focus:border-red-600 rounded w-full py-2 px-3 mr-4 text-black" %>
          <%= form.submit '+',class: "p-0 w-12 h-10 bg-red-500 rounded-full hover:bg-red-400 active:shadow-lg mouse shadow transition ease-in duration-200 focus:outline-none" %>
    
        </div>
      </div>
      <% end %>
     
    </div>
    