<% @category.errors.any? %>
  <ul>
    <% @category.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>




<div class="bg-gray-600 rounded shadow p-6 m-4 w-full lg:w-3/4 lg:max-w-lg md:max-w-2xl">
    <%@category.errors.any? %>
  <ul>
    <% @category.errors.full_messages.each do |msg| %>
      <li class="text-white"><%= msg %></li>
    <% end %>
  </ul>
      <div class="mb-4">
        <h1 class="text-white">Add Category</h1>
        <%= form_with model: @category do |form| %>
        <div class="flex-col mt-4">
        <%= form.label :description, class: "text-white" %>
        <%= form.text_field :description, class:"border border-gray-800 focus:border-blue-500 rounded w-full py-2 px-3 mr-4 text-black" %>
          <%= form.submit '+',class: "p-0 w-12 h-10 bg-gray-500 rounded-full hover:bg-gray-400 active:shadow-lg mouse shadow transition ease-in duration-200 focus:outline-none" %>
    
        </div>
      </div>
      <% end %>
     
    </div>
    